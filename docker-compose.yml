version: '3.8'

services:
  eval-runner:
    build: .
    container_name: eval-runner
    ports:
      - "8000:8000"
      - "8501:8501"
    volumes:
      - ./result:/app/result
      - ./.cache:/app/.cache
    restart: unless-stopped
